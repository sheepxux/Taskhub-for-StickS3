// Copy this file to `secrets.h` and fill in real values. secrets.h is gitignored.
#pragma once

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Mac Mini LAN address running server.py (Step 6 uses plain HTTP on the home LAN).
#define SERVER_HOST   "192.168.1.100"
#define SERVER_PORT   5577

#define DEVICE_ID     "sticks3-01"
// server.py has no auth yet; the token is sent in X-Device-Token but ignored.
#define DEVICE_TOKEN  "dev-token"
